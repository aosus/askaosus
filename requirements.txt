# Core dependencies
matrix-nio>=0.20.0
aiohttp>=3.8.0
openai>=1.0.0

# Utility dependencies  
python-dotenv>=1.0.0
markdown>=3.4.0  # For converting markdown to HTML in Matrix messages
# Development dependency for auto-reload
watchgod>=0.7.0
